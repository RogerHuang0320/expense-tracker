<nav class="navbar bg-light">
  <div class="container-fluid">
    <p class="navbar-brand">You are here to edit Expense.</p>
  </div>
</nav>

<div class="container">
  <form action="/records/{{record._id}}/edit?_method=PUT" method="POST">
    <div class="row">
      <div class="col-12 col-md-10 mx-auto">

        <div class="mb-3">
          <i class="fas fa-star fa-xs"></i>
          <label for="name">Expense Name</label>
          <input class="form-control" type="text" name="name" value="{{record.name}}" placeholder="消費名稱" required>
        </div>

        <div class="mb-3">
          <span class="text-secondary"><i class="fa fa-music"></i>
            Date
          </span>
          <input class="form-control" type="date" name="date" id="date" value="{{record.date}}">
        </div>

        <div class="mb-3">
          <span class="text-secondary"><i class="fas fa-solid fa-book-open"></i>
            Category
          </span>
          <select class="form-select mb-3" aria-label=".form-select-lg example" name="categoryId">
            <option selected value="{{record.categoryId}}">{{categoryName}}</option>
            {{#each categories}}
            <option value="{{this._id}}">{{this.name}}</option>
            {{/each}}
          </select>
        </div>

        <div class="mb-3">
          <span class="text-secondary"><i class="fas fa-solid fa-dollar-sign"></i>
            Price
          </span>
          <input class="form-control" type="number" name="amount" value="{{record.amount}}" placeholder="花了多少錢"
            required>
        </div>

        <div>
          <button class="btn btn-primary" type="submit">Done</button>
          <a class="btn btn-success" href="/">Back</a>
        </div>
      </div>
    </div>
  </form>
</div>